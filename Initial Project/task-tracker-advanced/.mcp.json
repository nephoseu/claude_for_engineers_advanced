{
  "//": "Both servers below are real, self-contained, local MCP servers, no external endpoints or credentials required. Source: .claude/mcp-servers/. Run 'npm install' inside .claude/mcp-servers/ once before the session (see docs/GETTING_STARTED.md). Participants should never need to author or edit this file themselves, they only review the permissions each server is scoped to (see .claude/mcp-servers.md).",
  "mcpServers": {
    "db-readonly": {
      "command": "node",
      "args": [".claude/mcp-servers/db-readonly/server.js"]
    },
    "logs-readonly": {
      "command": "node",
      "args": [".claude/mcp-servers/logs-readonly/server.js"]
    }
  }
}
