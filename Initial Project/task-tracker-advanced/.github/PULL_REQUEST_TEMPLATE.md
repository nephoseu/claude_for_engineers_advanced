## What this PR does

## Diagnosis
<!-- What was actually wrong, and how did you confirm it (logs / schema / reproduction)? -->

## Fix

## Tests
- [ ] A test exists that fails before this fix and passes after it
- [ ] Full suite passes locally (`npm test` in `backend/`)

## Migration review (if this PR includes a migration)
- [ ] Reversible or the destructive change is called out explicitly
- [ ] No mixed representations introduced
- [ ] Existing rows accounted for (default or backfill)
- [ ] Raw SQL was read by a human before running

## Automated PR review
<!-- Paste or link the findings from the pr-review workflow once it runs. -->
